import { noop } from '../../index.js';
export const derivedPromise = (executor) => {
    let resolve = noop;
    let reject = noop;
    const promise = new Promise((res, rej) => {
        resolve = res;
        reject = rej;
        (executor ?? noop)(res, rej);
    }).finally(() => {
        resolve = noop;
        reject = noop;
    });
    const controls = {
        resolve,
        reject,
    };
    return [
        promise,
        controls,
    ];
};
//# sourceMappingURL=derivedPromise.js.map