import { removeEventListener } from '../../index.js';
export const addEventListener = (element, eventName, fn, options) => {
    element.addEventListener(String(eventName), fn, options);
    return () => removeEventListener(element, eventName, fn);
};
//# sourceMappingURL=addEventListener.js.map