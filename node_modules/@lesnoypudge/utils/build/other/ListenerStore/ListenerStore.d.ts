import { T } from '@lesnoypudge/types-utils-base/namespace';
export type ListenerStoreCallback<Args extends unknown[]> = (T.AnyFunction<Args, void>);
export declare class ListenerStore<Key, Args extends unknown[]> {
    private store;
    constructor();
    add(key: Key, listener: ListenerStoreCallback<Args>): () => void;
    remove(key: Key, listener: ListenerStoreCallback<Args>): void;
    removeAll(): void;
    trigger(key: Key, ...args: Args): void;
    triggerAll(...args: Args): void;
    getSize(): number;
}
