export const pick = (source, ...keys) => {
    const output = keys.reduce((acc, cur) => {
        acc[cur] = source[cur];
        return acc;
    }, {});
    return output;
};
//# sourceMappingURL=pick.js.map