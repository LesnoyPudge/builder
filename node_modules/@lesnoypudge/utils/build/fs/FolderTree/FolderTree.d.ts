export declare namespace FolderTree {
    type File = {
        type: 'file';
        name: string;
        data: Buffer;
        path: string;
    };
    type Folder = {
        type: 'folder';
        name: string;
        files: File[];
        folders: Folder[];
        path: string;
    };
}
export declare class FolderTree {
    data: FolderTree.Folder | null;
    constructor(providedPath: string, extensions?: string[]);
    private createFolderTree;
    private createEmptyFolder;
    private fillFolder;
    private traverseFolder;
    traverse(cb: (value: FolderTree.Folder | FolderTree.File) => void): void;
    isEmpty(): boolean;
    getDataWithoutBuffer(): FolderTree.Folder | null;
}
