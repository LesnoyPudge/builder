export const getNameParts = (fileName) => {
    const splittedName = fileName.split('.');
    if (splittedName.length < 2)
        return null;
    const ext = splittedName.pop();
    if (!ext)
        return null;
    const name = splittedName.join('.');
    return {
        name,
        ext,
    };
};
//# sourceMappingURL=getNameParts.js.map