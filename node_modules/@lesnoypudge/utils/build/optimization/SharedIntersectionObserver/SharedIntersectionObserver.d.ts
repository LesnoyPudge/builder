import { ListenerStoreCallback } from '../../index.js';
type Args = [entry: IntersectionObserverEntry];
type StoreCallback = ListenerStoreCallback<Args>;
export declare class SharedIntersectionObserver {
    private listeners;
    private observers;
    private elementsToOptionsMap;
    constructor();
    private observerCallback;
    observe(element: Element, listener: StoreCallback, options?: IntersectionObserverInit): void;
    unobserve(element: Element, listener: StoreCallback): void;
    disconnect(): void;
}
export {};
