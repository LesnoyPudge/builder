import { ListenerStoreCallback } from '../../index.js';
type Args = [entry: ResizeObserverEntry];
type StoreCallback = ListenerStoreCallback<Args>;
export declare class SharedResizeObserver {
    private listeners;
    private observer;
    constructor();
    observe(element: Element, listener: StoreCallback, options?: ResizeObserverOptions): void;
    unobserve(element: Element, listener: StoreCallback): void;
    disconnect(): void;
}
export {};
