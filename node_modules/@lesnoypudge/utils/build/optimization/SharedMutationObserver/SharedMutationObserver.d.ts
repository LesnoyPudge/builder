import { ListenerStoreCallback } from '../../index.js';
type Args = [entry: MutationRecord];
type StoreCallback = ListenerStoreCallback<Args>;
export declare class SharedMutationObserver {
    private listeners;
    private observer;
    private elementsToOptionsMap;
    constructor();
    private processRecords;
    observe(element: Element, listener: StoreCallback, options?: MutationObserverInit): void;
    unobserve(element: Element, listener: StoreCallback): void;
    disconnect(): void;
}
export {};
