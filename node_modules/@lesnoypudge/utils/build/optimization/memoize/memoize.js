import { Cache } from '../../index.js';
export const memoize = (fn, lifespan = Infinity) => {
    const cache = new Cache();
    return (...args) => {
        return cache.getOrSet(args, () => fn(...args), lifespan);
    };
};
//# sourceMappingURL=memoize.js.map